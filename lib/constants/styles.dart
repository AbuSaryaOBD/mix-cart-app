import 'package:flutter/material.dart';

const Duration kAnimationDuration = Duration(milliseconds: 300);

final kRadiusEllipsis = BorderRadius.circular(50);
final kRadiusRounded1 = BorderRadius.circular(10);
final kRadiusRounded2 = BorderRadius.circular(25);
